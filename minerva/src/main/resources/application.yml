spring:
  profiles:

    active: ${profile}
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 56300

mybatis:
  configuration:
    map-underscore-to-camel-case: true